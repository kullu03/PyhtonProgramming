class BinHeap :
	def __init__(self):
		self.heapList = heapList[0]
		self.currentSize = 0

	def insert(self,k):
		self.heapList.append(k)
		self.currentSize = self.currentSize+1
		self.purcUp(self.currentSize)

	def percUp(self,i):
		while i //2 > 0 :
			
  
			
     
		

